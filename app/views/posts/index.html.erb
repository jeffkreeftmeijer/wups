<p id="notice"><%= notice %></p>

<h1>My wonderful weblog</h1>

<% @posts.each do |post| %>
  <div>
    <h2><%= link_to post.title, post %></h2>
    <p><%= post.body %></p>
    <small><%= post.created_at.to_s(:long) %>
      (<%= link_to 'Edit', edit_post_path(post) %>)
    </small>
  </div>
<% end %>

<%= link_to 'New Post', new_post_path %>
